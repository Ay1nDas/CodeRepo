queue<int> cities;
  // cities.push(v);

  // while (cities.size())
  // {
  //   int curr_city = cities.front();
  //   cities.pop();
  //   connected[curr_city]++;

  //   for (auto i : roads.at(curr_city))
  //   {
  //     if (connected[i] == 0)
  //       cities.push(i);
  //   }
  // }